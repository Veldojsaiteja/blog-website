<%- include("partials/header") -%>

<h1><b>Home</b></h1>

<% AllPosts.forEach(function(post){ %>
    <!-- <% console.log(post.heading + " " + post.content) %> -->
    <h1><%=post.title%></h1>
    <p><%=post.content.substring(0,100) + "..." %>
    <a href="/posts/<%= post._id %>"> Read More </a>
    </p>
<% }) %>

<%- include("partials/footer") -%>